program hello?;
var
 int : i&;
 begin
    if(x% && (z# || p%)) then
    begin
       write("memo");
       if(!y% || !a# && z% == l% && (x$ + 1 == 10) || (x# == z&)) then
       begin
          write("hello");
          if(2 && z# && ((x& < z#))) then
            begin
                 write("world");
            end
       end
    end
 end
